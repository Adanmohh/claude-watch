git_url("https://github.com/Adanmohh/ios-certificates.git")
storage_mode("git")

type("appstore")

app_identifier([
  "com.introinnovation.claudewatch",
  "com.introinnovation.claudewatch.watchkitapp"
])

# Non-interactive on CI: never prompt, fail fast if something is missing.
readonly(false)
